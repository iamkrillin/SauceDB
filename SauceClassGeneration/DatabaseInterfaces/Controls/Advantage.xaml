<UserControl x:Class="TheCodeHaven.SauceClassGeneration.DatabaseInterfaces.Controls.Advantage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0">Data Path</Label>
            <TextBox Grid.Row="0" Grid.Column="1" Name="DataPath" />

            <Label Grid.Row="1">Table Type</Label>
            <TextBox Grid.Row="1" Grid.Column="1" Name="TableType" />

            <Label Grid.Row="2">Server Type</Label>
            <TextBox Grid.Row="2" Grid.Column="1" Name="ServerType" />
        </Grid>
    </Grid>
</UserControl>
